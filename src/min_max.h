#ifndef MIN_MAX_H
#define MIN_MAX_H

#include <float.h>

int double_max_element(const double* const arr, const unsigned int len);

int double_min_element(const double* const arr, const unsigned int len);

#endif
